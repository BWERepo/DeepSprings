# Copy this file to .ftp-credentials (same directory, gitignored) and fill in
# the real password. deploy/ftp-deploy.sh reads this file at runtime if the
# FTP_HOST/FTP_USER/FTP_PASS environment variables aren't already set.
#
# KEY=VALUE, one per line, no quotes.
# Note: FTP_HOST is the server IP/hostname (hPanel > Hosting > Overview),
# NOT the domain name — the account's FTP home directory is already scoped
# to public_html/deepsprings.businesswebexpress.com.
FTP_HOST=82.197.83.134
FTP_USER=u541882440.deepsprings
FTP_PASS=
